// Self-hosted fork: update channel is disabled. Build from source to update.
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export async function CheckForUpdates(_force: boolean = false): Promise<void> {
  return;
}
